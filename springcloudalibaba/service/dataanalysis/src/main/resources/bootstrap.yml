spring:
  application:
    name: gateway
  cloud:
    nacos:
      config:
        server-addr: http://192.168.170.129:8848
        file-extension: yml

  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER